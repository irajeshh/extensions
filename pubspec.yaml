name: extensions
description: "A new Flutter package project."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.3.4

dependencies:
  intl: 0.18.1
  flutter:
    sdk: flutter

dev_dependencies:
  lintingpackage:
      path: ../lintingpackage
